# MyTrello
Lightweight To Do app built with React + TypeScript. Organize tasks in draggable columns, add tags and due dates, toggle dark mode, and track progress with a chart. Works offline with local persistence, import/export, optimistic updates, and undo/redo for safe edits.
